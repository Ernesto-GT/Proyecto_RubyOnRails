<%= form_with(model: category) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mx-2">
    <%= form.label :name,"Asignar Nombre", style: "display: block", class:"mx-2 text-zinc-700"%>
    <%= form.text_field :name, class:"w-full my-2 rounded-xl text-gray-600 bg-white border-zinc-600 text-xs sm:text-base", placeholder: 'Ej. Libros'  %>
  </div>
  <br>
  <div>
    <button class="bg-zinc-100 border-2 border-zinc-300 text-zinc-600 font-fold text-sm rounded-xl mx-4 my-5 p-1">
      Listo
    </button>
  </div>
<% end %>
